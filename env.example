# Database Configuration
# These values match the docker-compose.yaml file

# Postgres Settings
POSTGRES_HOST=localhost
POSTGRES_PORT=5433 #changed from 5432 because of localhost issue
POSTGRES_USER=user
POSTGRES_PASSWORD=pass
POSTGRES_DATABASE=trading

# Redis Settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Application Settings
APP_NAME="Trading & Margining System"
APP_VERSION=1.0.0
DEBUG=false

# Copy this file to .env and modify as needed for your environment
# cp env.example .env
